= @page.render

- content_for :sidebar do
  = link_to "Back", admin_pages_path, :class => "back"
  = link_to "Edit", edit_admin_page_path(@page), :class => "edit"
  = link_to "Delete", admin_page_path(@page), :method => :delete, :confirm => "Are you sure?", :class => "delete"