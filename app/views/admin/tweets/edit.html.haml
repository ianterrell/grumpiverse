= form_for [:admin, @tweet] do |f|
  = render :partial => "/admin/tweets/form", :locals => { :f => f }
  = f.submit
  = link_to "Cancel", admin_tweets_path

- content_for :sidebar do
  = link_to "Back", admin_tweets_path, :class => "back"
