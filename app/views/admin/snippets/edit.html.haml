= form_for [:admin, @snippet] do |f|
  = render :partial => "/admin/snippets/form", :locals => { :f => f }
  = f.submit
  = link_to "Cancel", admin_snippet_path(@snippet)

- content_for :sidebar do
  = link_to "Back", admin_snippet_path(@snippet), :class => "back"
  
